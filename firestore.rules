rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Rules are wide open for testing purposes.
    // This is not secure for a production environment.
    // Forcing a redeploy.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
